# Style settings

Six components for the six settings that live on the style rather than on a layer. All of them re-apply
themselves after a style switch — see [terrain, sky & globe](/guide/style-settings).

<ApiTable name="MglTerrain" />
<ApiTable name="MglSky" />
<ApiTable name="MglLight" />
<ApiTable name="MglProjection" />
<ApiTable name="MglImage" />
<ApiTable name="MglGlobalState" />
