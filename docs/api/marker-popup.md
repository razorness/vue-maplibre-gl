# Marker & popup

See [marker & popup](/guide/marker-and-popup) for how the two interact when nested.

<ApiTable name="MglMarker" />
<ApiTable name="MglPopup" />

## MglButton

A small helper for building controls with the same look as maplibre's own. Used by
`MglStyleSwitchControl` and the draw control, and public so you can build your own.

<ApiTable name="MglButton" />
